export const WaterVolumeFormatter = new Intl.NumberFormat('en-US', { style: 'unit', unit: 'milliliter', unitDisplay: 'narrow' })
export const RelativeTimeFormatter = new Intl.RelativeTimeFormat('en', { numeric: 'auto', style: 'long' })
